
| Key      | Value                       |
| -------- | --------------------------- |
| Name     | 林文跃                         |
| KeyWords | Compiler, AI Compiler，阅读，写作 |

我的兴趣首先是**阅读**（*历史*），其次是**写作**（*随笔和小说*），最后是**计算机**，我感兴趣的计算机领域是 `Complier`（*后来发现其实更喜欢 `Programming Language`，项目见[LISP-LLVM](https://github.com/shuishen-cloud/LISP-LLVM)*），最近正专注于 `AI Infra` 当中的 `AI Compiler`（*尤其是 `Compute Graph` 以及 `Kernel`，项目见[KernelVerify](https://github.com/shuishen-cloud/KernelVerify)* ）。
